
[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "profit-solver"
version = "0.0.1"
description = "Prototype for a search‑based profit maximization engine"
requires-python = ">=3.11"
